// import 'package:flutter/material.dart';

// class ContactServices {
//   static Future<List<Contact>> fetchContacts() async {
//     // Check for permission to access contacts
//     if (await FlutterContacts.requestPermission()) {
//       // Fetch all contacts and return them
//       List<Contact> contacts = await FlutterContacts.getContacts();
//       return contacts;
//     } else {
//       // If permission is denied, return an empty list
//       throw 'Permission denied to access contacts';
//     }
//   }
// }
